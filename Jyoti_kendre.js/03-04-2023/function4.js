let obj = {
    name:"chitra",
    age:23,
    character:function(str){
        console.log(str);
    }
}
console.log(obj.name);
console.log(obj.age);

//console-----console is object.
//log---------log is method of that object OR log is data (in side of data)

obj.character("chitra is brave mother")
// obj----------object
//character--------function of that object
